import { InputDecoratorDirective } from './input-decorator.directive';

describe('InputDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
