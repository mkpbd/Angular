import { SimpleTextDirective } from './simple-text.directive';

describe('SimpleTextDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleTextDirective();
    expect(directive).toBeTruthy();
  });
});
