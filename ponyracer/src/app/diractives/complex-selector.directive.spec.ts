import { ComplexSelectorDirective } from './complex-selector.directive';

describe('ComplexSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new ComplexSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
